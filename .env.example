#######################################################################
# Environment Configuration (Workspace Root)
#
# - Copy to `.env` and replace values
# - All values are placeholders by design: <<PLACEHOLDER>>
# - Variables include BOTH backend + frontend needs
#######################################################################

############################
# Runtime / Server
############################

NODE_ENV=<<development|test|production>>
PORT=<<3000>>

############################
# Backend: Mode Selection
############################

# In-memory mode
USE_INMEMORY=<<true|false>>

# AWS/LocalStack mode
USE_AWS=<<true|false>>

############################
# Backend: Firebase / Emulator
############################

FIREBASE_PROJECT_ID=<<tazco-financial-dev>>

# Firestore emulator (when using emulator mode)
FIRESTORE_EMULATOR_HOST=<<localhost:8080>>

# Firebase Auth emulator (used by backend/scripts/get-emulator-token.ts)
FIREBASE_AUTH_EMULATOR_HOST=<<localhost:9099>>

# Firebase tooling / tests
GCLOUD_PROJECT=<<demo-tazco>>

# Used by backend deploy scripts
FIREBASE_PROJECT_DEV=<<your-firebase-project-id>>

############################
# Backend: AWS / LocalStack
############################

AWS_ENDPOINT_URL=<<http://localhost:4566>>
AWS_REGION=<<us-east-1>>
AWS_ACCESS_KEY_ID=<<test>>
AWS_SECRET_ACCESS_KEY=<<test>>

COGNITO_USER_POOL_ID=<<us-east-1_localstack>>
COGNITO_CLIENT_ID=<<localstack-client-id>>

# Optional: event publishing to EventBridge/SQS
EVENTBRIDGE_BUS_NAME=<<tazco-financial-events>>
EVENT_SOURCE=<<tazco.financial-api>>
SQS_QUEUE_URL=<<http://localhost:4566/000000000000/tazco-financial-events>>
EVENT_PUBLISH_STRICT=<<true|false>>

############################
# Backend: Outbox
############################

# repository (default): allocate per-entity sequence numbers in repo
# caller: require callers to provide a non-zero sequenceNumber
OUTBOX_SEQUENCE_STRATEGY=<<repository|caller>>

############################
# Backend: Webhooks / WhatsApp (optional)
############################

WPP_BASE_URL=<<https://your-wpp-connect-host:21465>>
WPP_SECRET_KEY=<<wpp-secret-key>>
WPP_SESSION_NAME=<<tazco-financial-api>>

ADMIN_PHONE_1=<<E164_PHONE_NUMBER>>
ADMIN_PHONE_2=<<E164_PHONE_NUMBER>>

WEBHOOK_SECRET=<<RANDOM_LONG_SECRET>>
WHATSAPP_NOTIFICATIONS_ENABLED=<<true|false>>

############################
# Backend: Test/CLI helpers
############################

API_URL=<<http://localhost:3000>>
AUTH_TOKEN=<<OPTIONAL_TOKEN_FOR_CLI>>
ADMIN_API_KEY=<<OPTIONAL_ADMIN_API_KEY>>

############################
# Frontend (Next.js)
############################

NEXT_PUBLIC_API_URL=<<http://localhost:3000>>

# Optional: provide canned mock tokens (otherwise the frontend generates them)
NEXT_PUBLIC_DEV_USER_TOKEN=<<mock.base64.sig>>
NEXT_PUBLIC_DEV_ADMIN_TOKEN=<<mock.base64.sig>>
